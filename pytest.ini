[pytest]
testpaths = test
markers =
    real: runs a real program; skipped unless the configuration file gives it
        (--amac-config=PATH, else the config-amac.json of the repository).
filterwarnings =
    # NumPy 2.5 deprecations raised inside ASE itself; AMAC warnings stay visible.
    ignore::DeprecationWarning:ase.*
