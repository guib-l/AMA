"""Gaussian, registered as ``GAUSSIAN``.

Its handlers are exposed here (``from amac.assets import gaussian``, then
``gaussian.energy``) once implemented; none exists yet. The common handler
names are listed in "Adding a software" in the README.
"""
