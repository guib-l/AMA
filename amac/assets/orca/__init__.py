"""ORCA, registered as ``ORCA``.

Its handlers are exposed here (``from amac.assets import orca``, then
``orca.energy``) once implemented; none exists yet. The common handler
names are listed in "Adding a software" in the README.
"""
